import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  metadataBase: new URL("https://worldsignal.news"),
  title: {
    default: "WorldSignal - See Every Side of the Story",
    template: "%s | WorldSignal",
  },
  description:
    "WorldSignal compares world news coverage across the political spectrum so you can spot bias, blindspots, and emerging narratives.",
  keywords: ["geopolitics", "bias tracker", "news comparison", "ground news"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="bg-slate-950">
      <body
        className={`${geistSans.variable} ${geistMono.variable} bg-slate-950 text-slate-100 antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
